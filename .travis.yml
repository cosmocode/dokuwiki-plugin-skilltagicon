# Config file for travis-ci.org

language: php
php:
  - "5.5"
  - "5.4"
  - "5.3"
env:
  - DOKUWIKI=master
  - DOKUWIKI=stable
before_install:
 - wget https://raw.github.com/splitbrain/dokuwiki-travis/master/travis.sh
 - wget http://getcomposer.org/composer.phar
 - php composer.phar install --dev --no-interaction
install: sh travis.sh
script: cd _test && mkdir -p build/logs && phpunit --stderr --group plugin_skilltagicon --coverage-clover build/logs/clover.xml
after_script: php ../lib/plugins/skilltagicon/vendor/bin/coveralls -v
